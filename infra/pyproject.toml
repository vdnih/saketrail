[tool.poetry]
name = "saketrail-infra"
version = "0.1.0"
description = "Infrastructure as Code for SakeTrail"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
aws-cdk-lib = "^2.0.0"
constructs = "^10.0.0"
aws-cdk-aws-s3 = "^2.0.0"
aws-cdk-aws-cloudfront = "^2.0.0"
aws-cdk-aws-route53 = "^2.0.0"
aws-cdk-aws-certificatemanager = "^2.0.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0.0"
black = "^24.0.0"
isort = "^5.0.0"
flake8 = "^7.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api" 